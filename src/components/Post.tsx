import { Container } from '@mui/material';
import { Box } from '@mui/system';

const Post = () => {
  return (
    <>
      <Box
        sx={{
          width: '100%',
          height: 200,
          backgroundColor: 'green',
        }}
      />
    </>
  );
};

export default Post;
